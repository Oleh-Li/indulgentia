import React from "react";
// import styles from "./inputForm.module.css"

const InputForm = () => {
  return (
    <form action="submit">
      <input type="text" autoComplete="off" placeholder="input name" />
    </form>
  );
};

export default InputForm;
